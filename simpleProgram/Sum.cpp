#include <iostream>
using namespace std;
int main(){
    //taking user input
    int x,y,sum=0;
    cout<<"enter the two numbers that must be added";
    cin>> x;
    cin>> y;
    sum=x+y;
    cout<<"the sum is: "<<sum;
    return 0;
}